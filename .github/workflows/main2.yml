name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "He implementado un action en este repositorio en el evento push."
      - name: Running tests
        run: npm run test  
